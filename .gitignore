__pycache__/
*.pyc
venv/
.env
# Generated by build_static.py at Vercel deploy time from web/static/ — that's
# the single source of truth, this is just its build output.
public/
index/*
# The public demo has no server-side OpenAI key (see README, BYOK), so it
# ships with this pre-built index committed — regenerate + re-add deliberately
# with `python -m src.ingest && git add index/` after changing the docs.
!index/vectors.npy
!index/meta.json
.vercel
